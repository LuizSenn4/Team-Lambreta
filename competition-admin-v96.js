(() => {
  'use strict';
  const sb=window.teamSupabase;const list=document.getElementById('competitionApplicationsList');if(!sb||!list)return;
  let rows=[];const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const field=(label,value)=>value!==null&&value!==''?`<div><small>${label}</small><span>${esc(value)}</span></div>`:'';
  function render(){const q=document.getElementById('competitionApplicationSearch').value.trim().toLowerCase();const status=document.getElementById('competitionApplicationStatus').value;const filtered=rows.filter(r=>(status==='all'||r.status===status)&&(!q||[r.epic_nickname,r.full_name,r.discord,r.country,r.profiles?.game_nickname,r.profiles?.full_name].some(v=>String(v||'').toLowerCase().includes(q))));list.innerHTML=filtered.length?filtered.map(r=>`<article class="competition-admin-item" data-id="${r.id}"><button class="competition-admin-summary" type="button"><span class="competition-status status-${r.status}">${esc(r.status)}</span><strong>${esc(r.epic_nickname)}</strong><small>${esc(r.full_name)} · ${new Date(r.created_at).toLocaleString('pt-PT')}</small><b aria-hidden="true">⌄</b></button><div class="competition-admin-details" hidden><div class="competition-detail-grid">${field('Conta',r.profiles?.game_nickname||r.profiles?.full_name||r.user_id)}${field('Idade',r.age)}${field('País',r.country)}${field('Discord',r.discord)}${field('Plataforma',r.platform)}${field('Comando',r.input_method)}${field('Região',r.region)}${field('Build',r.build_preference)}${field('Modo',r.main_mode)}${field('PR',r.power_ranking)}${field('Tracker',r.tracker_links)}${field('Disponibilidade',r.availability)}${field('Experiência',r.competitive_experience)}${field('Motivação',r.motivation)}${field('Observações',r.notes)}</div>${r.status==='pendente'?`<footer><button data-review="aprovado">Aprovar</button><button class="danger" data-review="recusado">Recusar</button></footer>`:''}</div></article>`).join(''):'<p class="empty-admin">Nenhuma inscrição encontrada.</p>';}
  async function load(){const [{data,error},{data:profiles}]=await Promise.all([sb.from('competition_applications').select('*').order('created_at',{ascending:false}),sb.from('profiles').select('id,game_nickname,full_name')]);if(error){list.innerHTML=`<p class="empty-admin">${esc(error.message)}</p>`;return;}const profileMap=new Map((profiles||[]).map(p=>[p.id,p]));rows=(data||[]).map(row=>({...row,profiles:profileMap.get(row.user_id)||null}));render();}
  list.onclick=async e=>{const item=e.target.closest('[data-id]');if(!item)return;const review=e.target.closest('[data-review]');if(review){review.disabled=true;const {error}=await sb.rpc('review_competition_application',{p_application_id:Number(item.dataset.id),p_status:review.dataset.review});if(error){alert(error.message);review.disabled=false;}else await load();return;}if(e.target.closest('.competition-admin-summary')){const details=item.querySelector('.competition-admin-details');details.hidden=!details.hidden;}};
  ['competitionApplicationSearch','competitionApplicationStatus'].forEach(id=>document.getElementById(id).addEventListener(id.includes('Search')?'input':'change',render));document.getElementById('refreshCompetitionApplications').onclick=load;load();
})();

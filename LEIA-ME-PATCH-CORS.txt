TEAM LAMBRETA — PATCH V92.1 (TRADUÇÃO / CORS)

Este patch altera somente:
  supabase/functions/translate-message/index.ts

COMO APLICAR
1. Extraia o ZIP.
2. Copie a pasta "supabase" para a raiz do projeto Team Lambreta.
3. Confirme a substituição do arquivo index.ts.
4. No terminal, dentro da pasta do projeto, execute:

   npx supabase functions deploy translate-message --use-api

5. Atualize o site e teste o chat.

RESULTADO ESPERADO EM EDGE FUNCTIONS > INVOCATIONS
  OPTIONS 200
  POST 200

Não precisa executar SQL novamente.
Não precisa cadastrar a DEEPL_API_KEY novamente.
Não precisa fazer git push para publicar a Edge Function.

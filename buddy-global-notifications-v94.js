(()=>{'use strict';
const URL='https://ahiatqnokyhfpailobjx.supabase.co',KEY='sb_publishable_qgwMhZPrB_3cFv3yCMcToA_9nDvHz-O';
if(!window.supabase)return;
const sb=window.teamSupabase||window.supabase.createClient(URL,KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:false}});
let uid=null,channel=null,unread=0,profiles=new Map(),me=null,audio=null,audioUnlocked=false,audioCtx=null;
const SOUND_SRC=new URL('assets/sounds/buddy-message.wav',document.baseURI).href;
const soundKey=id=>`tl_buddy_sound_${uid||'anon'}_${id}`;
const soundEnabled=id=>localStorage.getItem(soundKey(id))==='1';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function busy(){return ['busy','ocupado','do-not-disturb','dnd'].includes(String(me?.presence||me?.presence_status||'').toLowerCase())}
function isBuddyRoute(){const path=location.pathname.replace(/\/+$/,'').toLowerCase();return document.body.classList.contains('buddy-page')||/(^|\/)buddy(?:\.html)?(?:\/|$)/.test(path)}
function getAudio(){if(!audio){audio=new Audio(SOUND_SRC);audio.preload='auto';audio.volume=.6}return audio}
function getAudioContext(){if(!audioCtx){const Ctx=window.AudioContext||window.webkitAudioContext;if(Ctx)audioCtx=new Ctx()}return audioCtx}
async function unlockAudio(){try{const ctx=getAudioContext();if(ctx&&ctx.state==='suspended')await ctx.resume();audioUnlocked=!!ctx&&ctx.state==='running';return audioUnlocked}catch(_){return false}}
async function playSynthSound(){try{const ctx=getAudioContext();if(!ctx)return false;if(ctx.state==='suspended')await ctx.resume();const now=ctx.currentTime;const gain=ctx.createGain();const osc=ctx.createOscillator();osc.type='sine';osc.frequency.setValueAtTime(880,now);osc.frequency.exponentialRampToValueAtTime(620,now+.12);gain.gain.setValueAtTime(.0001,now);gain.gain.exponentialRampToValueAtTime(.18,now+.01);gain.gain.exponentialRampToValueAtTime(.0001,now+.22);osc.connect(gain);gain.connect(ctx.destination);osc.start(now);osc.stop(now+.24);audioUnlocked=true;return true}catch(_){return false}}
async function playFileSound(){try{const a=getAudio();a.currentTime=0;a.volume=.6;await a.play();audioUnlocked=true;return true}catch(_){return false}}
async function playMessageSound(senderId,force=false){if(!force&&(!soundEnabled(senderId)||busy()))return false;return await playSynthSound()||await playFileSound()}
['pointerdown','touchend','keydown'].forEach(ev=>document.addEventListener(ev,unlockAudio,{once:true,passive:true}));
function ensureUi(){
 if(!document.getElementById('buddyGlobalToastStack')){const x=document.createElement('div');x.id='buddyGlobalToastStack';x.className='buddy-global-toast-stack';document.body.appendChild(x)}
 document.querySelectorAll('a,button').forEach(a=>{const text=(a.textContent||'').trim().toLowerCase(),href=a.getAttribute?.('href')||'',isCommunity=text==='comunidade'||text.includes('comunidade'),isBuddy=href.includes('buddy.html')||text==='buddy';if((isBuddy||isCommunity)&&!a.querySelector('.buddy-global-badge')){a.classList.add('buddy-badge-anchor');const b=document.createElement('span');b.className='buddy-global-badge';b.hidden=true;a.appendChild(b)}})
}
function paint(){ensureUi();document.querySelectorAll('.buddy-global-badge').forEach(b=>{b.textContent=unread>99?'99+':String(unread);b.hidden=unread<1});document.title=document.title.replace(/^\(\d+\)\s*/,'');if(unread>0)document.title=`(${unread}) ${document.title}`;window.dispatchEvent(new CustomEvent('buddy:unread',{detail:{count:unread}}))}
async function refresh(){if(!uid)return;const [{count},{data}]=await Promise.all([sb.from('private_messages').select('id',{count:'exact',head:true}).eq('receiver_id',uid).is('read_at',null).eq('hidden_by_receiver',false),sb.from('profiles').select('id,presence').eq('id',uid).maybeSingle()]);unread=count||0;me=data||me;paint()}
async function profile(id){if(profiles.has(id))return profiles.get(id);const {data}=await sb.from('profiles').select('id,full_name,game_nickname,game_nickname_public,avatar_url,custom_avatar_url').eq('id',id).maybeSingle();profiles.set(id,data||{});return data||{}}
async function toast(row){if(isBuddyRoute())return;const p=await profile(row.sender_id),n=p.game_nickname_public||p.game_nickname||p.full_name||'Buddy',av=p.custom_avatar_url||p.avatar_url||'';ensureUi();const box=document.getElementById('buddyGlobalToastStack'),el=document.createElement('button');el.type='button';el.className='buddy-global-toast';el.innerHTML=`${av?`<img src="${esc(av)}" alt="">`:''}<span><strong>${esc(n)}</strong><small>${esc((row.body||'Nova mensagem').slice(0,90))}</small></span>`;el.onclick=()=>location.href=`buddy.html?chat=${encodeURIComponent(row.sender_id)}`;box.appendChild(el);requestAnimationFrame(()=>el.classList.add('show'));setTimeout(()=>{el.classList.remove('show');setTimeout(()=>el.remove(),260)},5000)}
function observeMenus(){new MutationObserver(()=>ensureUi()).observe(document.documentElement,{childList:true,subtree:true})}
async function init(){ensureUi();observeMenus();const {data}=await sb.auth.getSession();uid=data.session?.user?.id;if(!uid)return;await refresh();channel=sb.channel(`buddy-global-${uid}-${Date.now()}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'private_messages',filter:`receiver_id=eq.${uid}`},payload=>{const r=payload.new||{};if(r.read_at)return;unread+=1;paint();playMessageSound(r.sender_id);toast(r)}).on('postgres_changes',{event:'UPDATE',schema:'public',table:'private_messages',filter:`receiver_id=eq.${uid}`},refresh).on('postgres_changes',{event:'UPDATE',schema:'public',table:'profiles',filter:`id=eq.${uid}`},payload=>{me=payload.new||me}).subscribe();window.addEventListener('focus',refresh);window.addEventListener('online',refresh);document.addEventListener('visibilitychange',()=>!document.hidden&&refresh());window.addEventListener('buddy:messages-read',refresh)}
window.TeamBuddyNotifications={soundEnabled,setSound:(id,on)=>{localStorage.setItem(soundKey(id),on?'1':'0');window.dispatchEvent(new CustomEvent('buddy:sound-changed',{detail:{id,on}}));if(on)unlockAudio()},testSound:()=>playMessageSound('',true),refresh};
init();
})();
